plugins {
    id 'java'
    id 'application'
    id 'org.openjfx.javafxplugin' version '0.0.13'
    id 'org.beryx.jlink' version '2.25.0'
}

group 'com.bitvault'
version '1.0'

repositories {
    mavenCentral()
}

ext {
    junitVersion = '5.9.0'
}

sourceCompatibility = '17'
targetCompatibility = '17'

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

application {
    mainModule = 'com.bitvault'
    mainClass = 'com.bitvault.Launcher'
}


javafx {
    version = '17'
    modules = ['javafx.controls', 'javafx.fxml']
}

dependencies {

    // argon2
    implementation group: 'de.mkammerer', name: 'argon2-jvm', version: '2.11'

    //needed for argon2
    implementation group: 'net.java.dev.jna', name: 'jna', version: '5.12.1'

    //json
    implementation group: 'com.fasterxml.jackson.core', name: 'jackson-databind', version: '2.13.4.2'

    //icons
    implementation group: 'org.kordamp.ikonli', name: 'ikonli-javafx', version: '12.3.1'
    implementation group: 'org.kordamp.ikonli', name: 'ikonli-materialdesign2-pack', version: '12.3.1'

    //sqlite
    implementation group: 'org.xerial', name: 'sqlite-jdbc', version: '3.39.2.0'

    //server
    implementation platform('io.netty:netty-bom:4.1.84.Final')
    implementation group: 'io.netty', name: 'netty-buffer', version: '4.1.84.Final'
    implementation group: 'io.netty', name: 'netty-codec', version: '4.1.84.Final'
    implementation group: 'io.netty', name: 'netty-common', version: '4.1.84.Final'
    implementation group: 'io.netty', name: 'netty-transport', version: '4.1.84.Final'
    implementation group: 'io.netty', name: 'netty-codec-http', version: '4.1.84.Final'
    // https://mvnrepository.com/artifact/io.projectreactor.tools/blockhound
    implementation group: 'io.projectreactor.tools', name: 'blockhound', version: '1.0.6.RELEASE'


//    // QR code
//    implementation 'com.google.zxing:core:3.5.0'
//    implementation 'com.google.zxing:javase:3.5.0'

    testImplementation("org.junit.jupiter:junit-jupiter-api:${junitVersion}")
    testRuntimeOnly("org.junit.jupiter:junit-jupiter-engine:${junitVersion}")
}

test {
    useJUnitPlatform()
}

jar {
    manifest {
        attributes 'Main-Class': 'com.bitvault.Launcher'
    }
}

jlink {

    //https://stackoverflow.com/questions/56512064/gradle-jlink-plugin-fails-during-createmergedmodule-with-the-service-implementa
//    mergedModule {
//        requires "java.xml"
//    }

    imageZip = project.file("${buildDir}/distributions/app-${javafx.platform.classifier}.zip") as RegularFile
    options = ['--strip-debug', '--compress', '2', '--no-header-files', '--no-man-pages']
    launcher {
        name = 'TheVault'
    }
}

jlinkZip {
    group = 'distribution'
}
jpackage {

}

task dist {
    dependsOn clean, jpackage
    description "Calls clean and then jpackage [default]"
}